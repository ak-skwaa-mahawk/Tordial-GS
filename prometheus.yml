global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "tordial_e8_mesh"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["127.0.0.1:8080"]
        labels:
          node_id: "TORDIAL-EDGE-01"
          cluster: "sovereign_mesh_ring"
